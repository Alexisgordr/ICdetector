Frequent cells can now accumulate trust evidence across the full 90-day window instead of being permanently limited by the latest 500 rows. The same correction applies to RF reconfiguration quarantine while all safety gates remain active, including the veto while the old pair is still visible. No database reset or migration is required; keep your existing history. Detection rules, scoring and schema 15 are unchanged.
