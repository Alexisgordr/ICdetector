Preserves evidence when a previously established cell transitions to changed with concrete trust contradictions. A neutral observation case reuses the existing prebuffer and post-capture window, records the real score and heuristics, avoids duplicates while the state persists, and is promoted if normal detection later starts an alarm. Detection, scoring, alerts, learning and the database schema are unchanged.
