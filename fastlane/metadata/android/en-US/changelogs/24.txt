Forensic capture now handles an already-contradicted first observation, reports failed writes and preserves whole closed cases during retention. Empty cases no longer suppress later evidence and SQLite foreign keys are enforced. H8, H11 and H14 now use the physical radio technology, so an LTE anchor is evaluated identically under 4G LTE and 5G NSA display labels. H6, scoring weights and database schema 15 are unchanged.
